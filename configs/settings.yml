# configs/settings.yml

environment:
  env_id: "LunarLander-v3"
  random_wind: false
  seed: 1

training:
  max_episodes: 1000

evaluation:
  n_episodes: 10
  enable_wind: false
  wind_power: 15.0

paths:
  video_folder: "results/videos/"
  model_folder: "results/models/"
